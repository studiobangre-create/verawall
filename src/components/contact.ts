// The contact address, in its own module so Footer, RedCta and
// DemoRequestForm can all import it without an import cycle.
// Requests go to the address published on the whitepaper and pitch deck.
export const CONTACT_EMAIL = 'contact@verawall.com';
